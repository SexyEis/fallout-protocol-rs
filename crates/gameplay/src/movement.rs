//! Player movement mechanics and state machine.

use bevy::prelude::*;

pub struct MovementPlugin;

impl Plugin for MovementPlugin {
    fn build(&self, _app: &mut App) {
        // todo: add movement systems
    }
}
