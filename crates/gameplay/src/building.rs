//! Voxel-based building system.

use bevy::prelude::*;

pub struct BuildingPlugin;

impl Plugin for BuildingPlugin {
    fn build(&self, _app: &mut App) {
        // todo: add building systems
    }
}
