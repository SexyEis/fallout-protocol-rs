//! Player inventory and stash management.

use bevy::prelude::*;

pub struct InventoryPlugin;

impl Plugin for InventoryPlugin {
    fn build(&self, _app: &mut App) {
        // todo: add inventory systems
    }
}
